// import { TestBed } from "@angular/core/testing";

// import { FuseOptionsBuilder } from "./fuse-options.service";
// import { FirebaseQueryBuilder } from "./firebase-query.service";

describe("FuseOptionsBuilder", () => {
	// beforeEach(() => TestBed.configureTestingModule({}));

	// it("should be created", () => {
	// 	const service: FuseOptionsBuilder = TestBed.inject(FuseOptionsBuilder);
	// 	expect(service).toBeTruthy();
	// });
});

describe("FirebaseQueryService", () => {
	// beforeEach(() => TestBed.configureTestingModule({}));

	// it("should be created", () => {
	// 	const service: FirebaseQueryService = TestBed.inject(FirebaseQueryService);
	// 	expect(service).toBeTruthy();
	// });
});
