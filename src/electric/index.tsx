import { ModuleContainer } from "@components";

// Chartjs Settings (Area Chart)
// fill: true,
// borderColor: 'rgb(75, 192, 192)',
// cubicInterpolationMode: 'monotone'
// tension: 0.4, <<< REMOVE THIS PROPERTY if 'monotone' interpolation

const Electric = () => {
  return (
    <ModuleContainer headerText="Electric Management">
      <div></div>
    </ModuleContainer>
  );
};

export default Electric;
