version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.12-browsers
        environment:
          CHROME_BIN: "/usr/bin/google-chrome"
    branches:
      only:
        - master
        - ngx
    steps:
      - checkout

      - run:
          name: Installing dependenices
          command: yarn install --frozen-lockfile

      - run:
          name: Run testing and upload code coverage to Codecov
          command: yarn test --token=$CODECOV_TOKEN

      - run:
          name: Building source code
          command: yarn build

      - run:
          name: Deploying to Firebase
          command: ./node_modules/.bin/firebase deploy --project "rin-anidb" --only hosting --token=$FIREBASE_DEPLOY_TOKEN --non-interactive

